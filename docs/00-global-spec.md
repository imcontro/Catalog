# Global Spec v0.3

## 1. Назначение документа

Этот документ является главным spec проекта **Каталог NapitkiBerkat**.

Он фиксирует общее понимание проекта:

- для кого создается проект;
- какую проблему он решает;
- какие роли есть в проекте;
- какой основной сценарий должен пройти клиент;
- какие крупные части входят в первую версию;
- какие главные правила нельзя нарушать при дальнейшем проектировании.

Детальное поведение отдельных частей описывается в feature specs. Техническая реализация, дизайн, база данных, API, фреймворки и конкретная верстка будут описываться позже в technical specs и skills.

Проект ведется по правилу: сначала spec, потом plan, потом code. Если требования нет в spec, оно не делается. Если требование неясно, оно уточняется. Если требование противоречит spec, работа останавливается до уточнения.

## 2. Название проекта

Название проекта: **Каталог NapitkiBerkat**.

## 3. Суть проекта

Проект представляет собой каталог-сайт для оптового магазина напитков NapitkiBerkat.

Клиент получает ссылку на сайт, открывает ассортимент, смотрит цены, выбирает нужные товары и отправляет заказ в WhatsApp.

Главная идея: заменить ручную отправку ассортимента, цен и уточнений одним удобным каталогом, который помогает клиенту быстро понять ассортимент, цену, доступность товара, доставку и способ оформления заказа.

## 4. Для кого проект

Основные клиенты:

- кафе;
- рестораны;
- клиенты, которые заказывают напитки на дом;
- другие покупатели напитков.

Домашние клиенты идут по тому же сценарию, что кафе, рестораны и другие клиенты: открывают каталог, выбирают товары и отправляют заказ в WhatsApp.

Проект ориентирован не на случайный розничный просмотр, а на клиентов, которые уже заинтересовались покупкой и хотят быстро увидеть доступный ассортимент.

## 5. Основная проблема

Сейчас клиент часто спрашивает ассортимент и цены отдельно.

Показывать весь ассортимент вручную сложно, долго и неудобно:

- нужно каждый раз объяснять, какие товары доступны, а каких сейчас нет;
- нужно отправлять цены;
- нужно отвечать на повторяющиеся вопросы;
- клиенту сложно сравнивать товары;
- заказ может теряться в переписке;
- менеджер тратит много времени на однотипную коммуникацию.

## 6. Цели проекта

Клиентская цель:

- быстро увидеть ассортимент напитков;
- понять цены;
- найти товар по названию;
- выбрать нужные товары;
- сформировать заказ;
- увидеть сумму товаров, подсказку по бесплатной доставке и информацию по доставке;
- отправить заказ в WhatsApp.

Бизнес-цель:

- быстрее закрывать интерес клиента;
- уменьшить ручной труд при показе ассортимента;
- упростить процесс заказа;
- упростить понимание доставки;
- повысить вероятность, что клиент дойдет до заказа;
- дать владельцу удобный способ менять товары, цены, фото, вкусы, категории, сортировку в **Все напитки**, сортировку внутри категории и статус товара без разработки.

## 7. Роли проекта

### 7.1. Клиент

Клиент открывает каталог по ссылке, смотрит ассортимент, выбирает товары, указывает данные заказа и отправляет заказ в WhatsApp.

### 7.2. Владелец каталога

Владелец управляет каталогом через админку.

Он должен иметь возможность без разработки менять товары, цены, фото, вкусы, список категорий, категорию товара, сортировку в **Все напитки**, сортировку внутри категории, статус товара и удалять товары.

### 7.3. Работник магазина

Работник получает заказ в WhatsApp и должен быстро понять:

- какие товары заказали;
- какие вкусы выбраны;
- сколько упаковок нужно;
- какая сумма товаров;
- какая информация по доставке;
- куда доставлять;
- как клиент хочет оплатить заказ.

## 8. Основной сценарий клиента

1. Клиент пишет или звонит и интересуется ассортиментом.
2. Ему отправляют ссылку на каталог.
3. Клиент открывает каталог на телефоне или компьютере.
4. Клиент видит раздел "Все напитки" и может фильтровать товары по категориям.
5. Клиент выбирает товары в каталоге.
6. Клиент собирает заказ в корзине.
7. Клиент видит сумму товаров и подсказку по бесплатной доставке.
8. Клиент открывает оформление заказа.
9. Клиент указывает адрес доставки.
10. Клиент видит итоговую информацию по доставке.
11. Клиент выбирает способ оплаты.
12. Клиент нажимает "Отправить в WhatsApp".
13. В WhatsApp формируется понятное сообщение с заказом.
14. Работник магазина получает заказ и обрабатывает его в чате.

Основной канал передачи ссылки: WhatsApp или другая переписка с клиентом.

Приоритет устройства: **мобильный телефон**.

Сайт также должен нормально работать на компьютере.

## 9. Карта feature specs

Детальные правила первой версии описаны в отдельных feature specs:

- `01-client-catalog.feature-spec.md` - клиентский каталог, категории, карточки товаров, вкусы, поиск и видимость товаров;
- `02-cart.feature-spec.md` - корзина, количество упаковок, суммы, недоступные позиции и удаление товаров;
- `03-checkout-and-whatsapp.feature-spec.md` - оформление заказа, обязательные данные и WhatsApp-сообщение;
- `04-delivery.feature-spec.md` - правила отображения доставки;
- `05-admin-auth.feature-spec.md` - вход в админку;
- `06-admin-products.feature-spec.md` - управление товарами, категориями, вкусами, статусами, фото, ценами и сортировкой;
- `07-empty-and-error-states.feature-spec.md` - пустые, ошибочные и нестандартные состояния.

Global spec не должен дублировать все детали этих документов. Если нужна точная логика отдельной части, нужно смотреть соответствующий feature spec.

## 10. Главные правила первой версии

### 10.1. Каталог и товары

В первой версии категории управляются владельцем через админку.

При первом запуске можно использовать стартовый список категорий:

- вода;
- газировки;
- лимонады;
- чай;
- натуральные напитки;
- энергетики;
- разные напитки.

Клиентский каталог показывает только товары, которые должны быть видны клиенту.

На первом экране каталога выбран фильтр **Все напитки**.

**Все напитки** показывает все видимые товары каталога и не является отдельной категорией товара.

Клиент может фильтровать список товаров по категориям, заданным владельцем.

**Все напитки** не входит в список категорий и не редактируется как категория.

Фильтр **Все напитки** всегда показывается первым перед категориями.

В **Все напитки** товары показываются в отдельном ручном порядке, заданном владельцем.

Внутри каждой категории товары показываются в отдельном ручном порядке, заданном владельцем.

Порядок в **Все напитки** и порядок внутри категории могут отличаться.

Товар может иметь вкусы. Если у товара несколько вкусов, это одна карточка товара с выбором вкуса.

Поиск клиента работает только по названию товара и действует внутри выбранного фильтра:

- если выбран фильтр **Все напитки**, поиск идет по всем видимым товарам;
- если выбрана категория, поиск идет только по видимым товарам этой категории.

Объем, тара или формат товара указываются в названии товара, если они важны для клиента. Отдельного поля объема в первой версии нет.

Количество штук в одной уп обязательно для клиентского каталога.

Если у товара все вкусы отмечены как "нет в наличии", это не отдельный статус товара, а вычисляемое состояние. Товар остается видимым клиенту, но недоступен для заказа, потому что все его вкусы недоступны.

Точные остатки товара в штуках не показываются и не ведутся.

Единица заказа: **упаковка**. На сайте и в WhatsApp упаковка отображается сокращенно как **уп**.

### 10.2. Статусы товара

У товара есть четыре основных статуса:

- доступен для заказа;
- нет в наличии;
- скрыт из каталога;
- черновик.

У товара должен быть один основной статус.

Товар без основного фото, цены, количества штук в одной уп или категории можно сохранить как черновик, но нельзя показывать клиенту.

Скрытые товары и черновики видны владельцу в отдельных списках админки, но не видны клиенту.

### 10.3. Корзина и заказ

Клиент выбирает количество упаковок через плюс и минус, без ручного ввода количества текстом.

Сумма товаров считается только по доступным позициям.

Если товар или выбранный вкус стал "нет в наличии" после добавления в корзину, позиция остается видимой в корзине, но не участвует в отправке заказа.

Если товар был скрыт после добавления в корзину, он исчезает из корзины.

Если товар был удален после добавления в корзину, он исчезает из корзины.

Адрес доставки и способ оплаты обязательны для отправки заказа.

Имя клиента и телефон отдельно не спрашиваются.

Способы оплаты в первой версии:

- наличные;
- перевод.

Онлайн-оплата на сайте не входит в первую версию.

### 10.4. Доставка

Детальные правила доставки описаны в `04-delivery.feature-spec.md`.

В корзине показывается подсказка по бесплатной доставке по г. Грозный.

На оформлении заказа и в WhatsApp-сообщении показывается итоговая информация по доставке.

Порог бесплатной доставки: 8000 рублей.

Порог считается только по доступным позициям, которые реально отправляются в заказ.

Сайт не проверяет город в адресе доставки и не рассчитывает точную стоимость доставки по адресу.

### 10.5. WhatsApp

Заказ отправляется только в WhatsApp.

WhatsApp-сообщение должно быть коротким и понятным работнику магазина.

В WhatsApp-сообщение попадают только доступные позиции.

Сообщение должно включать:

- приветствие;
- компактный список товаров;
- выбранный вкус, если он есть;
- количество уп;
- сумму товаров;
- информацию по доставке;
- адрес доставки;
- способ оплаты.

### 10.6. Админка

Админка нужна владельцу для управления каталогом без разработки.

Вход в админку: один общий логин и один общий пароль.

Сложная система ролей не нужна.

В клиентском каталоге не должно быть видимой ссылки или кнопки перехода в админку.

Владелец должен иметь возможность управлять товарами, ценами, фото, вкусами, статусами, списком категорий, категорией товара, сортировкой в **Все напитки**, сортировкой внутри категории и удалением товаров.

Обычный список товаров в админке показывает активные товары и товары со статусом "нет в наличии". Скрытые товары и черновики открываются в отдельных списках.

## 11. Что не входит в первую версию

В первую версию не входят:

- регистрация клиентов;
- личный кабинет клиента;
- разные цены для разных клиентов;
- минимальный заказ;
- акции и скидки;
- избранные товары;
- онлайн-оплата на сайте;
- автоматическая синхронизация со складом;
- точные остатки товара в штуках;
- сложная CRM;
- сложная система ролей и прав доступа;
- полноценный интернет-магазин с оплатой на сайте.

Эти возможности можно рассмотреть позже, если они понадобятся.

## 12. Нестандартные состояния

Пустые, ошибочные и нестандартные состояния описаны отдельно в `07-empty-and-error-states.feature-spec.md`.

Global spec фиксирует только общий принцип: пользователь не должен попадать в тупик. Если действие невозможно, сайт должен объяснить причину и следующий шаг официальным, понятным текстом.

## 13. Критерии успеха первой версии

Первая версия считается успешной, если:

- клиент может открыть ссылку на телефоне;
- клиент видит раздел "Все напитки";
- клиент может фильтровать товары по категориям;
- клиент видит товары, фото и цены;
- клиент может найти товар по названию;
- клиент может выбрать вкус товара, если у товара есть несколько вкусов;
- клиент может выбрать несколько товаров;
- клиент может менять количество упаковок через плюс и минус;
- клиент видит товары в порядке, заданном владельцем для **Все напитки** и для выбранной категории;
- клиент видит сумму товаров;
- клиент видит подсказку по бесплатной доставке в корзине;
- клиент видит информацию по доставке на оформлении заказа;
- клиент может указать адрес доставки;
- клиент может выбрать способ оплаты;
- клиент может отправить заказ в WhatsApp;
- WhatsApp-сообщение понятно работнику магазина;
- владельцу не нужно вручную перечислять весь ассортимент каждому клиенту;
- владелец может через админку менять товары, цены, фото, вкусы, количество штук в одной уп, список категорий, категорию товара, сортировку в **Все напитки**, сортировку внутри категории, статус товара и удалять товары.
