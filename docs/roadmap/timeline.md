# Общая Хронология Работ

Этот файл хранит хронологическую историю завершенных work plans.

Новые записи добавляются сверху, чтобы последние изменения были видны первыми.

## Записи

### 2026-06-24 - Админка: вход

- Work plan: `docs/work-plans/completed/2026-06-24-admin-auth.work-plan.md`
- Ветка: `feature/admin-auth`
- Pull Request:
- Области: admin

Сделано:

- Подключена рабочая форма входа в админку `/admin/login`.
- Добавлены API `POST /api/admin/login` и `POST /api/admin/logout`.
- Реализована серверная админская сессия через `httpOnly` cookie.
- В таблице `admin_sessions` хранится хеш токена сессии, а не сам токен.
- Закрытая страница `/admin` проверяет сессию на сервере и без входа переводит на `/admin/login`.
- Добавлена кнопка выхода из админки.
- В technical spec зафиксирован формат `ADMIN_PASSWORD_HASH`.

Проверено:

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- `npm.cmd run build`
- `npm.cmd run db:check`
- `git diff --check`
- Ручной сценарий входа, защиты `/admin`, выхода и повторного открытия `/admin` после выхода.
- Владелец попросил сделать push.

Заметки:

- Управление товарами, категориями, вкусами, фото и сортировкой не входило в этот work plan.
- Тестовые локальные значения `.env` не входят в Git.

### 2026-06-24 - Формат WhatsApp-сообщения без эмодзи с жирными подписями

- Work plan: `docs/work-plans/completed/2026-06-18-whatsapp-message-format.work-plan.md`
- Ветка: `feature/whatsapp-message-format`
- Pull Request:
- Области: cart-checkout-whatsapp

Сделано:

- Обновлен формат WhatsApp-сообщения заказа: постоянные подписи выделены жирным через WhatsApp-разметку `*...*`.
- Приветствие, блок товаров, сумма, доставка, адрес и оплата оформлены без эмодзи и декоративных маркеров.
- Строки товаров оставлены компактными в формате `Название товара вкус - 1уп`.
- Значения заказа оставлены обычным текстом.
- Значение доставки в WhatsApp-сообщении больше не дублирует подпись `Доставка:`.
- Обновлены связанные feature spec, technical spec и user story.

Проверено:

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- `npm.cmd run build`
- `git diff --check`
- Владелец попросил сделать push.

Заметки:

- Бизнес-логика корзины, доставки, актуализации позиций и очистки корзины не менялась.
- Поля оформления заказа, номер WhatsApp, Supabase, админка, миграции и API не менялись.

### 2026-06-18 - Редизайн клиентского интерфейса

- Work plans:
  - `docs/work-plans/completed/2026-06-17-client-interface-paper-design.work-plan.md`
  - `docs/work-plans/completed/2026-06-17-reference-layout-draft.work-plan.md`
  - `docs/work-plans/completed/2026-06-18-client-interface-reference-layout-transfer.work-plan.md`
- Ветка: `feature/client-interface-redesign`
- Pull Request:
- Области: catalog, cart-checkout-whatsapp, frontend, design

Сделано:

- Создана черновая страница `/design-draft/reference-layout` для визуального утверждения нового клиентского интерфейса.
- Утвержденный reference layout перенесен в основной каталог `/`, корзину `/cart` и окно оформления заказа.
- Обновлены карточки товаров, поиск, категории, нижняя кнопка корзины, страница корзины и оформление заказа.
- Сохранена текущая бизнес-логика каталога, корзины, доставки и WhatsApp-сообщения.
- Обновлены связанные specs и user story под утвержденный визуальный подход.

Проверено:

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- `npm.cmd run build`
- `git diff --check`
- Владелец проверил утвержденный дизайн и попросил сделать push.

Заметки:

- Админка, Supabase, миграции, формат WhatsApp-сообщения и бизнес-логика заказа не менялись.

### 2026-06-14 - Страница корзины и окно оформления заказа

- Work plan: `docs/work-plans/completed/2026-06-14-cart-page-checkout-modal.work-plan.md`
- Ветка: `feature/cart-page-checkout-modal`
- Pull Request:
- Области: cart-checkout-whatsapp, frontend, catalog, design

Сделано:

- Добавлена отдельная публичная страница корзины `/cart`.
- Полный состав корзины перенесен из окна на страницу корзины с фото, названием, выбранным вкусом, ценой, количеством и суммой позиции.
- Фото позиции берется из выбранного вкуса, если оно есть, иначе используется основное фото товара.
- В каталоге компактная кнопка корзины ведет на `/cart` и остается видимой даже при пустой корзине.
- После добавления товара в каталоге показывается дополнительная кнопка **Корзина** со счетчиком уп.
- Оформление заказа перенесено из отдельной страницы в почти полноэкранное окно поверх `/cart`.
- Старый маршрут `/checkout` удален без редиректа.
- Корзина актуализируется через `POST /api/cart/resolve`, скрытые и удаленные позиции удаляются, недоступные остаются видимыми и не входят в сумму.
- После открытия WhatsApp-ссылки корзина очищается.
- Обновлены feature specs, technical specs и user story под путь `Каталог -> /cart -> окно оформления -> WhatsApp`.

Проверено:

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- `npm.cmd run build`
- `git diff --check`
- Владелец проверил локальный сценарий корзины, оформления заказа и попросил сделать push.

Заметки:

- Заказ не сохраняется в базе данных.
- Онлайн-оплата, админка, деплой на Vercel и изменение схемы базы данных не входили в этот work plan.

### 2026-06-12 - Оформление заказа и WhatsApp

- Work plan: `docs/work-plans/completed/2026-06-12-checkout-whatsapp.work-plan.md`
- Ветка: `feature/checkout-whatsapp`
- Pull Request:
- Области: cart-checkout-whatsapp, frontend, design

Сделано:

- Добавлена страница оформления заказа `/checkout`.
- Из корзины добавлен переход **Оформить заказ**, доступный только при наличии доступных позиций.
- На оформлении заказа показываются выбранные позиции, недоступные позиции, сумма доступных товаров и итоговая строка доставки.
- Корзина актуализируется через `POST /api/cart/resolve` при открытии оформления и повторно перед отправкой в WhatsApp.
- Добавлены обязательный адрес доставки с примером `Грозный ул. Назабраева 92` и обязательный выбор оплаты: наличные или перевод.
- Реализовано формирование WhatsApp-сообщения с приветствием, компактным списком товаров, суммой, доставкой, адресом и оплатой.
- В WhatsApp-сообщение попадают только доступные позиции, а недоступные позиции остаются видимыми на оформлении и не участвуют в сумме.
- После открытия WhatsApp корзина очищается.
- Формат WhatsApp-сообщения оставлен без эмодзи, чтобы на устройствах клиента и работника не появлялись вопросительные знаки.
- Обновлены specs, technical spec, user story и active/completed work plan под финальный формат сообщения.

Проверено:

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- `npm.cmd run build`
- `git diff --check`
- HTTP 200 для `/` и `/checkout`
- Владелец проверил локальное оформление заказа, WhatsApp-сообщение и попросил сделать push.

Заметки:

- Заказ не сохраняется в базе данных.
- Онлайн-оплата, админка и деплой на Vercel не входили в этот work plan.
- Полный редизайн клиентского интерфейса остается отдельным будущим work plan.

### 2026-06-12 - Корзина клиента

- Work plan: `docs/work-plans/completed/2026-06-11-cart.work-plan.md`
- Ветка: `feature/cart`
- Pull Request:
- Области: cart-checkout-whatsapp, frontend, catalog, design

Сделано:

- Добавлено добавление доступных товаров и выбранных вкусов в корзину из клиентского каталога.
- Корзина хранится на клиенте в `localStorage` с ключом `napitki_berkat_cart` и сохраняется после обновления страницы.
- На карточке товара после добавления показывается управление количеством: минус, количество `уп` и плюс.
- Добавлена компактная кнопка открытия корзины с иконкой, текстом **Корзина** и счетчиком выбранных уп.
- Корзина открывается отдельным окном, а на телефоне - нижней панелью с крупными кнопками управления.
- В корзине показываются позиции, выбранные вкусы, цена, количество, сумма позиции и общая сумма товаров.
- Добавлены удаление позиции, очистка всей корзины и автоматическое закрытие корзины после удаления последней позиции.
- Добавлен серверный `POST /api/cart/resolve` для актуализации корзины по данным Supabase.
- Скрытые и удаленные позиции удаляются из корзины при актуализации, недоступные позиции остаются видимыми, но не участвуют в сумме.
- Добавлена подсказка по бесплатной доставке по г. Грозный от суммы `8000` руб.
- В design spec зафиксировано направление будущего визуального оформления по примерам владельца для телефона и компьютера.

Проверено:

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- `npm.cmd run build`
- `git diff --check`
- Владелец проверил локальную корзину и попросил сделать push.

Заметки:

- Этот план не включает оформление заказа, ввод адреса, выбор оплаты, WhatsApp-сообщение и очистку корзины после WhatsApp.
- Полный редизайн клиентского интерфейса по предоставленным примерам остается отдельным следующим work plan.

### 2026-06-06 - Клиентский каталог на данных Supabase

- Work plan: `docs/work-plans/completed/2026-06-06-client-catalog.work-plan.md`
- Ветка: `feature/client-catalog`
- Pull Request:
- Области: catalog, frontend, design

Сделано:

- Главная страница `/` показывает клиентский каталог на реальных данных тестового Supabase.
- Добавлен серверный `GET /api/catalog` и серверная сборка DTO каталога без служебных ключей и секретов.
- Категории, товары, вкусы, цены, фото, статусы и сортировка берутся из таблиц `categories`, `products`, `product_flavors` и `images`.
- Клиентский каталог показывает только видимые товары и не показывает черновики, скрытые и удаленные товары.
- Добавлены фильтр **Все напитки**, фильтры категорий, поиск по названию товара и выбор вкусов.
- При выборе вкуса карточка меняет цену и фото, если такие данные есть, а выбранный вкус показывается в названии цветной вставкой.
- Карточки товаров адаптированы под мобильную ширину, фото ограничены рамкой и не растягивают карточку.
- Названия категорий, товаров и вкусов отображаются с заглавной буквы.

Проверено:

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- `npm.cmd run build`
- `npm.cmd run db:check`
- `npm.cmd run catalog:import:check`
- `git diff --check`
- Владелец проверил локальный каталог на `http://127.0.0.1:3000` и попросил сделать push.

Заметки:

- `npm.cmd run catalog:import:check` подтвердил 7 категорий, 86 товаров, 108 вкусов, 82 images и 82 файла в Storage.
- Публичные ссылки фото открываются для 82 из 82 импортированных фото.
- Изменения не включают корзину, оформление заказа, WhatsApp, вход в админку и деплой на Vercel.

### 2026-06-06 - Загрузка данных старого каталога в Supabase

- Work plan: `docs/work-plans/completed/2026-06-06-catalog-data-import.work-plan.md`
- Ветка: `feature/catalog-data-import`
- Pull Request:
- Области: data-supabase

Сделано:

- В подтвержденный тестовый Supabase загружены реальные данные старого каталога.
- В Supabase Postgres записаны 86 товаров и 108 вкусов.
- В Supabase Storage bucket `product-images` загружены 82 фото в папку `old-catalog/`.
- Добавлена команда проверки результата импорта `npm.cmd run catalog:import:check`.
- Подготовлен отчет `data/import/old-catalog-import-result-report.md`.
- Обновлен preparation-report после повторного dry-run: повторный запуск обновляет существующие записи, а не создает дубли.

Проверено:

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- `npm.cmd run build`
- `npm.cmd run storage:ensure`
- `npm.cmd run catalog:import:validate`
- `npm.cmd run catalog:import:dry-run`
- `npm.cmd run db:check`
- `npm.cmd run catalog:import:check`
- `git diff --check`
- Владелец проверил результат и попросил сделать push.

Заметки:

- `npm.cmd run catalog:import:check` подтвердил 7 категорий, 86 товаров, 108 вкусов, 82 images и 82 файла в Storage.
- Публичные ссылки открываются для 82 из 82 импортированных фото.
- 4 товара остались черновиками из-за отсутствующего основного фото: `горная 5л`, `К-13`, `родники 0,5л`, `южания 0.2 детский`.
- Боевой Supabase не использовался в рамках этого work plan.

### 2026-06-06 - Подготовка импорта старого каталога

- Work plan: `docs/work-plans/completed/2026-06-06-catalog-import-preparation.work-plan.md`
- Ветка: `feature/catalog-import-preparation`
- Pull Request:
- Области: data-supabase

Сделано:

- Добавлена валидация draft-данных старого каталога.
- Добавлен dry-run импорта без записи в Supabase.
- Подготовлен маппинг категорий старого каталога на категории проекта.
- Добавлена проверка фото, черновиков, дублей товаров и дублей вкусов.
- Подготовлен отчет `data/import/old-catalog-import-preparation-report.md`.
- Реальная запись в Supabase защищена отдельным confirm-флагом и не запускалась в рамках этого plan.

Проверено:

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- `npm.cmd run build`
- `npm.cmd run catalog:import:validate`
- `npm.cmd run catalog:import:dry-run`
- `git diff --check`
- Владелец проверил результат и попросил сделать push.

Заметки:

- Dry-run показал 7 найденных категорий, 82 images к созданию, 86 товаров к созданию и 108 вкусов к созданию.
- 82 товара готовы к клиентскому каталогу, 4 товара останутся черновиками из-за отсутствующего основного фото.
- Финальная загрузка реальных товаров и фото в Supabase остается отдельным следующим work plan.

### 2026-06-06 - Supabase и база данных

- Work plan: `docs/work-plans/completed/2026-06-06-supabase-database.work-plan.md`
- Ветка: `feature/supabase-database`
- Pull Request:
- Области: data-supabase

Сделано:

- Выбран и зафиксирован Drizzle ORM для работы с Supabase Postgres.
- Добавлены зависимости, конфигурация Drizzle и серверные модули подключения к базе.
- Подготовлены схема и миграция для таблиц `categories`, `products`, `product_flavors`, `images` и `admin_sessions`.
- Добавлены стартовые категории каталога.
- Добавлена серверная проверка чтения из базы.
- Подготовлен Supabase Storage bucket `product-images`.
- Обновлен `.env.example` без реальных секретов.

Проверено:

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- `npm.cmd run build`
- `npm.cmd run db:migrate`
- `npm.cmd run db:check`
- `npm.cmd run storage:ensure`
- `git diff --check`
- Проверено, что локальный `.env` игнорируется Git.

Заметки:

- Реальные товары и фото старого каталога не загружались.
- Клиентский каталог, корзина, WhatsApp-заказ и админка управления товарами не входили в этот work plan.
- Pull Request и merge владелец делает сам на GitHub.

### 2026-06-05 - Подготовка приложения

- Work plan: `docs/work-plans/completed/2026-06-05-application-foundation.work-plan.md`
- Ветка: `feature/application-foundation`
- Pull Request:
- Области: frontend, design, admin, catalog, docs-process

Сделано:

- Создана основа Next.js / React / TypeScript приложения.
- Добавлены базовые страницы `/` и `/admin/login`.
- Добавлены общие стили, использование логотипа и русские тексты интерфейса.
- Добавлены `.env.example`, `.gitignore`, TypeScript, ESLint и Next.js конфиги.
- Настроен `npm run dev` через Turbopack, чтобы локальный dev-сервер стабильно запускался на этой машине.

Проверено:

- Владелец открыл сайт в браузере и подтвердил, что все нормально.
- `npm run lint`
- `npm run typecheck`
- `npm run build`
- `npm audit --audit-level=high`
- HTTP 200 для `/` и `/admin/login`.
- `git diff --check`

Заметки:

- Supabase, реальные товары, корзина, WhatsApp и настоящая авторизация админки не входят в этот work plan.

## Шаблон записи

```text
### YYYY-MM-DD - Название work plan

- Work plan: `docs/work-plans/completed/file-name.work-plan.md`
- Ветка: `branch-name`
- Pull Request: `PR link or empty`
- Области: frontend, design, admin

Сделано:

- ...

Проверено:

- ...

Заметки:

- ...
```
